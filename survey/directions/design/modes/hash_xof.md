# 哈希与可扩展输出 (Hash & XOF)

## 方向概述

基于对称密码原语的哈希函数和可扩展输出函数（XOF）设计，包括海绵结构、Merkle-Damgård 结构及其变体。

## 关键问题

- 轻量级哈希函数的安全性下界
- 海绵结构的安全性证明
- XOF 在后量子密码中的应用

## 代表方案

SHA-3/Keccak, Ascon-Hash, PHOTON, SPONGENT

## 论文列表

（见 `papers.yaml` 中 `directions` 含 `hash_xof` 的条目）
